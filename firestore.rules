rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All access is server-side only (Cloud Run service accounts).
    // No client-side (browser) access is allowed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
